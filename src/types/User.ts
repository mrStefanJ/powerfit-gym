export interface User {
    id: string;
    firstName?: string;
    lastName?: string;
    email: string;
    password: string;
    birthDate?: string;
    gender?: string;
    image?: string;
    group?: string; 
}